{% extends 'base.html.twig' %}

{% block title %}Proprio{% endblock %}

{% block body %}
    <h1>Proprio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proprio.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ proprio.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_proprio_index') }}">back to list</a>

    <a href="{{ path('app_proprio_edit', {'id': proprio.id}) }}">edit</a>

    {{ include('proprio/_delete_form.html.twig') }}
{% endblock %}
