<form method="post" action="{{ path('app_chenille_delete', {'id': chenille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chenille.id) }}">
    <button class="btn">Delete</button>
</form>
