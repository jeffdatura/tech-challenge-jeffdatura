{% extends 'base.html.twig' %}

{% block title %}Accueil propriétaire{% endblock %}

{% block body %}
<h1>Accueil propriétaire</h1>


    <table class="table table-bordered border-dark">


        <thead class="table-dark"></thead>

            <tr>
              <th>identifiant</th>
              <th>Nom de l'animal</th>
              <th>Race</th>
              <th>Propriétaire</th>
              <th>Chenille</th>
            </tr>
        </thead>
        <tbody>
        {% for animal in animals %}
            <tr>
                <td>{{ animal.id }}</td>
                <td>{{ animal.nom }}</td>
                <td>{{ animal.race }}</td>
                <td>{{ animal.proprio }}</td>
                <td>{{ animal.chenille }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_animal_show', {'id': animal.id}) }}">show</a>

                    <a class="btn btn-primary" href="{{ path('app_animal_edit', {'id': animal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_animal_new') }}">Create new</a>
{% endblock %}
